targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            daymark: lib/core/database/daymark_database.dart
          schema_dir: drift_schemas/
          test_dir: test/database/migrations/
